# GFI1B pipeline config for Snakemake orchestration (relative paths).

data:
  meta: GFI1B_run/meta_cis_GFI1B.csv
  counts: GFI1B_run/gene_counts.csv
  counts_read_csv_kwargs: {index_col: 0}

model:
  modality_name: gene
  cis_gene: GFI1B
  output_dir: output
  label: testing_20260224
  guide_covariates: [cell_line]

run:
  steps: [technical, cis]

technical:
  set_technical_groups: [cell_line]
  fit:
    tolerance: 0
    niters: 50000
  save: true

cis:
  load_technical:
    enabled: true
  adjust_ntc_sum_factor:
    enabled: true
    args:
      covariates: [lane, cell_line]
  fit:
    technical_covariates: [cell_line]
    sum_factor_col: sum_factor_adj
    tolerance: 0
    niters: 100000
    predictive_on_cpu: true
  save: true
